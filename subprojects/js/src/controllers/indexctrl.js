goog.provide('zoosk.controllers.IndexCtrl');

/**
 * Controller for handling authentication from the login page. If authentication is successful, a redirect will be performed
 * to the 'session' page else a dialog will be shown to the user with the appropriate error message.
 *
 * @param {angular.Scope} $scope
 */
zoosk.controllers.IndexCtrl = function($scope) {

};
